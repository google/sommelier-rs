diff --git a/sommelier/src/handler/compositor.rs b/sommelier/src/handler/compositor.rs
index 0101f44..7a97b3d 100644
--- a/sommelier/src/handler/compositor.rs
+++ b/sommelier/src/handler/compositor.rs
@@ -20,7 +20,7 @@ use crate::protocols::wayland::wl_subcompositor::WlSubcompositorHandler;
 use crate::protocols::wayland::wl_subsurface::WlSubsurfaceHandler;
 use crate::protocols::wayland::wl_surface::WlSurfaceHandler;
 use crate::state::{Context, SurfaceState};
-use crate::wire::Action;
+use crate::wire::{Action, MessageBuilder};
 use log::debug;
 use std::cmp;
 use std::ptr;
@@ -112,6 +112,34 @@ impl WlSurfaceHandler for CompositorHandler {
         }
         Action::Forward
     }
+
+    fn on_damage_buffer(
+        &mut self,
+        ctx: &mut Context,
+        x: i32,
+        y: i32,
+        width: i32,
+        height: i32,
+    ) -> Action {
+        let surface_guest_id = ctx.last_sender_id;
+        if let Some(surface_host_id) = ctx.shadow_table.get_host_id(surface_guest_id) {
+            let mut builder = MessageBuilder::new();
+            builder.write_i32(x);
+            builder.write_i32(y);
+            builder.write_i32(width);
+            builder.write_i32(height);
+
+            let mut full_msg = Vec::new();
+            full_msg.extend_from_slice(&surface_host_id.to_ne_bytes());
+            let len = (builder.payload.len() + 8) as u32;
+            let word2 = (len << 16) | (2u32); // wl_surface::REQ_DAMAGE (opcode 2)
+            full_msg.extend_from_slice(&word2.to_ne_bytes());
+            full_msg.extend_from_slice(&builder.payload);
+
+            ctx.client_to_host_queue.push((full_msg, Vec::new()));
+        }
+        Action::Drop
+    }
 }
 
 impl WlRegionHandler for CompositorHandler {}
diff --git a/sommelier/src/handler/registry.rs b/sommelier/src/handler/registry.rs
index f96571d..3c5dbcf 100644
--- a/sommelier/src/handler/registry.rs
+++ b/sommelier/src/handler/registry.rs
@@ -40,6 +40,30 @@ impl wl_registry::WlRegistryHandler for RegistryHandler {
         // Track host globals
         ctx.host_globals.insert(interface.clone(), name);
 
+        if interface == "wl_compositor" && version < 4 {
+            let client_version = 4;
+            let mut global_builder = MessageBuilder::new();
+            global_builder.write_u32(name);
+            global_builder.write_string(interface);
+            global_builder.write_u32(client_version);
+
+            // Translate host registry ID to guest registry ID
+            let registry_guest_id = ctx
+                .shadow_table
+                .get_guest_id(ctx.last_sender_id)
+                .unwrap_or(ctx.last_sender_id);
+
+            let mut global_msg = Vec::new();
+            global_msg.extend_from_slice(&registry_guest_id.to_ne_bytes());
+            let len = (global_builder.payload.len() + 8) as u32;
+            let word2 = (len << 16) | (wl_registry::EVT_GLOBAL as u32);
+            global_msg.extend_from_slice(&word2.to_ne_bytes());
+            global_msg.extend_from_slice(&global_builder.payload);
+            ctx.host_to_client_queue.push((global_msg, Vec::new()));
+
+            return Action::Drop;
+        }
+
         if interface == "zwp_linux_dmabuf_v1" {
             if !ctx.gpu_accel {
                 return Action::Drop;
@@ -261,7 +285,11 @@ impl wl_registry::WlRegistryHandler for RegistryHandler {
             let mut builder = MessageBuilder::new();
             builder.write_u32(_name);
             builder.write_string(interface);
-            builder.write_u32(*_version);
+            let mut bind_version = *_version;
+            if interface == "wl_compositor" && bind_version > 3 {
+                bind_version = 3;
+            }
+            builder.write_u32(bind_version);
             builder.write_u32(host_new_id);
 
             let mut full_msg = Vec::new();
diff --git a/sommelier/src/handler/text_input.rs b/sommelier/src/handler/text_input.rs
index 45d5df5..c419b87 100644
--- a/sommelier/src/handler/text_input.rs
+++ b/sommelier/src/handler/text_input.rs
@@ -50,6 +50,18 @@ impl zwp_text_input_v1::ZwpTextInputV1Handler for TextInputV1Handler {
             msg.extend_from_slice(&word2.to_ne_bytes());
             msg.extend_from_slice(&builder.payload);
             ctx.host_to_client_queue.push((msg, Vec::new()));
+
+            // v3 done (opcode 5)
+            let mut done_builder = crate::wire::MessageBuilder::new();
+            done_builder.write_u32(0); // serial
+
+            let mut done_msg = Vec::new();
+            done_msg.extend_from_slice(&guest_id.to_ne_bytes());
+            let done_len = (done_builder.payload.len() + 8) as u32;
+            let done_word2 = (done_len << 16) | 5u32;
+            done_msg.extend_from_slice(&done_word2.to_ne_bytes());
+            done_msg.extend_from_slice(&done_builder.payload);
+            ctx.host_to_client_queue.push((done_msg, Vec::new()));
         }
         Action::Drop
     }
@@ -187,10 +199,44 @@ impl zwp_text_input_v1::ZwpTextInputV1Handler for TextInputV1Handler {
 
     fn on_delete_surrounding_text(
         &mut self,
-        _ctx: &mut Context,
-        _index: i32,
-        _length: u32,
+        ctx: &mut Context,
+        index: i32,
+        length: u32,
     ) -> Action {
+        let host_id = ctx.last_sender_id;
+        if let Some(guest_id) = ctx.shadow_table.get_guest_id(host_id) {
+            let before_length = if index < 0 { (-index) as u32 } else { 0 };
+            let after_length = if index + (length as i32) > 0 {
+                (index + (length as i32)) as u32
+            } else {
+                0
+            };
+
+            // v3 delete_surrounding_text (opcode 4)
+            let mut builder = crate::wire::MessageBuilder::new();
+            builder.write_u32(before_length);
+            builder.write_u32(after_length);
+
+            let mut msg = Vec::new();
+            msg.extend_from_slice(&guest_id.to_ne_bytes());
+            let len = (builder.payload.len() + 8) as u32;
+            let word2 = (len << 16) | 4u32;
+            msg.extend_from_slice(&word2.to_ne_bytes());
+            msg.extend_from_slice(&builder.payload);
+            ctx.host_to_client_queue.push((msg, Vec::new()));
+
+            // v3 done (opcode 5)
+            let mut done_builder = crate::wire::MessageBuilder::new();
+            done_builder.write_u32(0); // serial
+
+            let mut done_msg = Vec::new();
+            done_msg.extend_from_slice(&guest_id.to_ne_bytes());
+            let done_len = (done_builder.payload.len() + 8) as u32;
+            let done_word2 = (done_len << 16) | 5u32;
+            done_msg.extend_from_slice(&done_word2.to_ne_bytes());
+            done_msg.extend_from_slice(&done_builder.payload);
+            ctx.host_to_client_queue.push((done_msg, Vec::new()));
+        }
         Action::Drop
     }
 
@@ -386,32 +432,37 @@ impl zwp_text_input_v3::ZwpTextInputV3Handler for TextInputV3Handler {
 
             if state.enabled_changed {
                 if state.enabled {
-                    // activate: opcode 0
-                    let mut builder = crate::wire::MessageBuilder::new();
-                    builder.write_u32(host_seat);
-                    builder.write_u32(host_surface);
-
-                    let mut full_msg = Vec::new();
-                    full_msg.extend_from_slice(&host_v1_id.to_ne_bytes());
-                    let len = (builder.payload.len() + 8) as u32;
-                    let word2 = len << 16;
-                    full_msg.extend_from_slice(&word2.to_ne_bytes());
-                    full_msg.extend_from_slice(&builder.payload);
-                    ctx.client_to_host_queue.push((full_msg, Vec::new()));
+                    if host_seat != 0 && host_surface != 0 {
+                        // activate: opcode 0
+                        let mut builder = crate::wire::MessageBuilder::new();
+                        builder.write_u32(host_seat);
+                        builder.write_u32(host_surface);
+
+                        let mut full_msg = Vec::new();
+                        full_msg.extend_from_slice(&host_v1_id.to_ne_bytes());
+                        let len = (builder.payload.len() + 8) as u32;
+                        let word2 = len << 16;
+                        full_msg.extend_from_slice(&word2.to_ne_bytes());
+                        full_msg.extend_from_slice(&builder.payload);
+                        ctx.client_to_host_queue.push((full_msg, Vec::new()));
+                        state.enabled_changed = false;
+                    }
                 } else {
-                    // deactivate: opcode 1
-                    let mut builder = crate::wire::MessageBuilder::new();
-                    builder.write_u32(host_seat);
-
-                    let mut full_msg = Vec::new();
-                    full_msg.extend_from_slice(&host_v1_id.to_ne_bytes());
-                    let len = (builder.payload.len() + 8) as u32;
-                    let word2 = (len << 16) | 1u32;
-                    full_msg.extend_from_slice(&word2.to_ne_bytes());
-                    full_msg.extend_from_slice(&builder.payload);
-                    ctx.client_to_host_queue.push((full_msg, Vec::new()));
+                    if host_seat != 0 {
+                        // deactivate: opcode 1
+                        let mut builder = crate::wire::MessageBuilder::new();
+                        builder.write_u32(host_seat);
+
+                        let mut full_msg = Vec::new();
+                        full_msg.extend_from_slice(&host_v1_id.to_ne_bytes());
+                        let len = (builder.payload.len() + 8) as u32;
+                        let word2 = (len << 16) | 1u32;
+                        full_msg.extend_from_slice(&word2.to_ne_bytes());
+                        full_msg.extend_from_slice(&builder.payload);
+                        ctx.client_to_host_queue.push((full_msg, Vec::new()));
+                        state.enabled_changed = false;
+                    }
                 }
-                state.enabled_changed = false;
             }
 
             if let Some((text, cursor, anchor)) = state.surrounding_text.take() {
diff --git a/sommelier/src/state.rs b/sommelier/src/state.rs
index d57821f..d9aeb70 100644
--- a/sommelier/src/state.rs
+++ b/sommelier/src/state.rs
@@ -58,6 +58,7 @@ impl ShadowTable {
     }
 
     pub fn map_id(&mut self, guest_id: u32, host_id: u32) {
+        log::trace!("map_id: guest={} -> host={}", guest_id, host_id);
         if let Some(old_host_id) = self.guest_to_host.insert(guest_id, host_id) {
             if old_host_id != host_id {
                 self.host_to_guest.remove(&old_host_id);
@@ -75,10 +76,12 @@ impl ShadowTable {
     }
 
     pub fn track_interface(&mut self, guest_id: u32, interface: String) {
+        log::trace!("track_interface: guest={} -> {}", guest_id, interface);
         self.interfaces.insert(guest_id, interface);
     }
 
     pub fn track_host_interface(&mut self, host_id: u32, interface: String) {
+        log::trace!("track_host_interface: host={} -> {}", host_id, interface);
         self.host_interfaces.insert(host_id, interface);
     }
 
@@ -91,6 +94,7 @@ impl ShadowTable {
     }
 
     pub fn remove_id(&mut self, guest_id: u32) {
+        log::trace!("remove_id: guest={}", guest_id);
         if let Some(host_id) = self.guest_to_host.remove(&guest_id) {
             self.host_to_guest.remove(&host_id);
             self.host_interfaces.remove(&host_id);
